{
  "name": "kvitt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "DEVBOX=true concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "DEVBOX=true concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm start --prefix backend\" \"npm run preview --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  },
  "dependencies": {
    "all": "^0.0.0"
  }
}
