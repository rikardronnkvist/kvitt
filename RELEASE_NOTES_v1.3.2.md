# Kvitt 1.3.2

## Improvements

- Renamed the integrations page to `/my-apps`. The old `/api-tokens` page URL redirects to the new address, preserving existing bookmarks.
- Expanded the Claude Desktop connection guide with an updated walkthrough and a second in-app demo image.

## Compatibility

- No backend API changes. Existing personal-token endpoints and OAuth grants are unchanged.